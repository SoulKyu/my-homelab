---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: test-claim-nfs-pool01
  annotations:
    volume.beta.kubernetes.io/storage-class: "freenas-nfs-csi-pool1"
spec:
  storageClassName: freenas-nfs-csi-pool1
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi